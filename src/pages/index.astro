---
import { supabase } from '@/lib/supabase';

const { data: users, error } = await supabase
  .from('users')
  .select('id, text, body');

if (error) {
  console.error(error);
}
---

<Layout>
  <ul>
    {users?.map(u => <li>{u.text}</li>)}
  </ul>
</Layout>